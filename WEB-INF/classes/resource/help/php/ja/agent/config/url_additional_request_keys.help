基本的にWebアプリケーションは、リクエストURLがアプリケーションサービス名になりますが、アプリケーションサービスがURLだけでは分類できない場合が増えています。
このような場合、リクエストのパラメータを使ってアプリケーションサービス名を具体的に設定できます。JENNIFERエージェントのurl_additional_request_keysオプションで、該当のリクエストパラメータ名を設定します。


    key1
    key2

前の場合、以下のコードからリターンするパラメータの値を使用してアプリケーションサービス名を決定します。

    request.getParameter(”key1”);
    request.getParameter(”key2”);

例えば、特定リクエストに関するアプリケーションサービス名は以下のように決定されます。

    http://127.0.0.1:8080/app.php?key1=a&key2=b  => /app.php+(key1=a&key2=b)
    http://127.0.0.1:8080/app.php?key1=a         => /app.php+(key1=a)
    http://127.0.0.1:8080/app.pgp?key1=a&key2=   => /app.php+(key1=a)
    http://127.0.0.1:8080/app.pgp                => /app.jsp

パラメータ名が長い場合の**:**を使用して一部のみ使用できます。

    key1:5

設定値がパラメータ値よりも長い場合には適用できません。パラメータが80文字を超える場合は、80文字まで適用されます。

上記のように設定すると、次のコードがリターンする値がアプリケーションサービス名に追加されます。

    request.getParameter("key1").substring(0, 5)

---

* デフォルト値：なし
* タイプ：文字列
* サンプル値

    parameter1
    parameter2
* 再起動：不要
* サポートバージョン：5.0.0