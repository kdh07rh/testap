애플리케이션 이름 설정시 쿼리 스트링으로 서비스 이름을 설정 할 수 있도록 하는 옵션이다.

기본적 형태는 서비스 이름 뒤에 javax.servlet.http.HttpServletRequest.getQueryString() 메소드의 호출 값을 '+' 기호를 사용해 연결한다.

    ex) /example/serviceName+queryString

---

* 기본값 : false
* 형식 : Boolean
* 재시작 : 안함
* 지원 버전 : 5.2.2.2