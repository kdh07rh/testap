이 옵션을 통해 추적하고 싶은 HTTP Request Parameter의 키를 설정하면 트랜잭션의 프로파일 정보를 통해 그 값을 확인할 수 있다.

샘플값에 설정된 내용을 통해 트랜잭션의 프로파일에서 아래와 같은 정보들을 확인할 수 있다.

     HTTP-PARAM : id=test; email=test@example.com

---

* 기본값 : 없음
* 형식 : 문자열(줄바꿈을 이용하여 복수 설정이 가능함)
* 샘플값

        id
        email

* 재시작 : 안함
* 연관 옵션
    * [profile_http_value_length](./agentHelp?key=profile_http_value_length&type=1)
* 지원 버전 : 5.6.1.20