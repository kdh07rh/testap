HTTP Header 에 포함된 값을 이용하여 기본 애플리케이션명에 추가하여 설정할 경우 사용하는 옵션이다.
관리화면을 통해 일반적인 설정이 가능하나 특정 상황에 HTTP Header 값을 이용하여 애플리케이션명에 추가적으로 설정하고자 할 경우 사용한다.

HTTP Header 내용이 다음과 같을 경우 애플리케이션명을 추가 설정하는 예제이다.

    HTTP Header = { user=Tester, guid=1234567890 }

    옵션에 HTTP Header 키를 입력한다.(줄바꿈을 이용하여 여러개 설정이 가능)
    user
    guid

    index.php 요청시 예상되는 애플리케이션명
    ex) /index.php+Tester+1234567890


사용시 주의 할 점은 애플리케이션명 설정을 관리 화면과 중복 설정 할 경우 관리 화면에서 설정하는 값이 우선순위가 높아 HTTP Header 를 이용해 추가 설정된 애플리케이션명을 덮어 쓰게 된다.

---

* 기본값 : 없음
* 형식 : 문자열
* 샘플값

    header_1
    header_2

* 재시작 : 안함
* 연관 옵션
    * [service_naming_by_http_header](./agentHelp?key=service_naming_by_http_header&type=3)
* 지원 버전 : 5.5.0.3