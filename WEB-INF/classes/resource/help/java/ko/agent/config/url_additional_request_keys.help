기본적으로 웹 애플리케이션은 요청 URI가 애플리케이션 서비스 이름이 되지만 웹 애플리케이션 프레임워크의 발전으로 애플리케이션 서비스가 URI만으로는 구분되지 않는 경우가 많아지고 있다.

이런 경우에는 요청 파라미터를 이용해서 애플리케이션 서비스 이름을 구체적으로 설정할 수 있다.

    key1
    key2

앞의 경우에는 다음 코드가 반환하는 파라미터 값을 사용해서 애플리케이션 서비스 이름을 결정한다.

    request.getParameter(”key1”);
    request.getParameter(”key2”);

예를 들어, 특정 요청에 대한 애플리케이션 서비스 이름은 다음과 같이 결정된다.

    http://127.0.0.1:8080/app.jsp?key1=a&key2=b  => /app.jsp+(key1=a&key2=b)
    http://127.0.0.1:8080/app.jsp?key1=a         => /app.jsp+(key1=a)
    http://127.0.0.1:8080/app.jsp?key1=a&key2=   => /app.jsp+(key1=a)
    http://127.0.0.1:8080/app.jsp                => /app.jsp

파라미터 값이 길 경우의 콜론(**:**)을 사용하여 시작 일부만을 사용할 수도 있다.

    key1:5

만약 설정한 값이 파라미터 값 보다 긴 경우에는 적용되지 않으며 파라미터가 길이가 최대 80을 넘을 경우 80자까지만 적용된다.

앞에서와 같이 설정하면 다음 코드가 반환하는 값을 애플리케이션 서비스 이름에 추가한다.

    request.getParameter("key1").substring(0, 5)



##**주의 사항**##
해당 옵션을 설정하는 경우 지원하는 환경에 따라 비정상 동작할 수 있다.
옵션을 적용하기전에 반드시 확인이 필요한 옵션이다.

---

* 기본값 : 없음
* 형식 : 문자열
* 샘플값

        parameter1
        parameter2

* 재시작 : 안함
* 연관 옵션
    * [ignore_url_post_request_parsing_prefix](./agentHelp?key=ignore_url_post_request_parsing_prefix&type=1)
    * [request_set_character_encoding](./agentHelp?key=request_set_character_encoding&type=1)
* 지원 버전 : 5.0.0