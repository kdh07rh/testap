HTTP 서비스에 대한 트랜잭션 추적시 기본 추적 방법을 사용자 정의한다.

    public interface IAdapterHttpService 
    {
        bool reject(Object request, Object response, Object activeObject);
        bool rejectMessage(Object request, Object response, Object activeObject);
        void start(Object request, Object response, Object activeObject);
        void end(Object request, Object response, Object activeObject, Exception t);
        void interceptServicePerformance(Object xvpoint);
    }

메소드 설명
---------------
reject(Object request, Object response, Object activeObject);
: HTTP 서비스를 reject 처리한다. PLC 처리가 일반적이지만 사용자 IP나 혹은 기타 다른 입력값을 기준으로 PLC 기능을 처리하고자 할 경우에 사용자 정의한다. 리턴값이 true 이면 HTTP 서비스 요청이 reject 된다.

rejectMessage(Object request, Object response, Object activeObject);
: HTTP 서비스 요청이 reject될 때 사용자 정의 메시지를 보낼 경우 재정의한다. 리턴값이 true이면 사용자에게 이미 적절한 메시지를 보냈다는 의미이다.

    bool b = rejectMessage(request, response, o);
    if (b == false)
    {
        // 어댑터에서 재정의하지 않았으면,
        // 제니퍼 닷넷이 정의한 기본 메세지 처리 진행
    }

start(Object request, Object response, Object activeObject);
: HTTP 서비스가 시작될 때 별도 추적 정보를 초기화할 때 사용한다.

end(Object request, Object response, Object activeObject, Exception t);
: HTTP 서비스가 종료된 시점에 제니퍼로 추적한 정보를 검증하거나 변형하려고 할 때 사용한다.

interceptServicePerformance(Object xvpoint);
: 서비스 성능 정보(XViewPoint)를 서버에 전송하기 직전에 사용된다.


**설정방법**

어댑터를 구현한 클래스 소스 코드 파일을 [제니퍼설치폴더]/extension 폴더에 저장한 후 extension 폴더에 이미 있는 verify_script.bat 파일을 사용해 유효성을 검증한다. 유효성 검증에 성공하면 제니퍼 콘솔을 통해 고급 옵션에서 adapter_http_service 항목에 검증된 소스 코드의 파일명을 설정한다.

---
    
* 기본값 : 없음
* 형식 : 문자열
* 샘플값 : ServiceHttpAdapter.cs 
* 재시작 : 필요하지만, adapter_revision 옵션 값을 재설정하면 실행 시에 반영됨
* 연관 옵션
    * [adapter_revision](./agentHelp?key=adapter_revision&type=2)
* 지원 버전 : 5.0.8.1