제니퍼 서버는 모든 문자열을 utf-8로 처리한다. 따라서 PHP 에이전트는 수집한 문자열을 utf-8로 변환해 전송해야 한다. 이를 위해 제니퍼 PHP Agent관리자는 현 PHP 어플리케이션의 encoding값을 current_encoding으로 설정해야 한다. 

utf-8로 변환 하는 문자열 대상은 아래와 같다.

1. 메소드 Parameter 문자열
2. 메소드 Return 문자열 
3. SQL Parameter 문자열

---

* 기본값 : utf-8
* 형식 : 문자열
* 샘플값 : euc-kr
* 재시작 : 안함
* 지원버전 : 5.0.0