프레임웍 혹은 애플리케이션에서 자체 제작한 Connection Pool을 사용하는 경우 해당 DB Connection Pool 에서 DB Connection을 획득할 때 사용하는 메소드를 지정한다.

db_connection_get에 설정되는 메소드는 반드시 java.sql.Connection을 리턴해야한다.

    db.DBConnectionManager getConnection


Class 이름만 설정할 경우 해당 클래스 안에 있는 메소드들 중에 java.sql.Connection을 리턴하는 모든 메소드가 설정된다.

    db.DBConnectionManager


---

* 기본값 : 없음
* 형식 : 문자열(줄바꿈을 이용하여 복수 설정이 가능함)
* 샘플값

        db.DBManager getConnection
        db.DBConnectionManager getConnection
        db.DataSource

* 재시작 : 필요
* 연관 옵션
    * [db_connection_close](./agentHelp?key=db_connection_close&type=1)
    * [enable_jdbc_bootstrap_wrapper_factory](./agentHelp?key=enable_jdbc_bootstrap_wrapper_factory&type=1)
* 지원 버전 : 5.0.0