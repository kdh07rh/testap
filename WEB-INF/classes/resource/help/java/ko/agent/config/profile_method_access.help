메소드 프로파일을 설정을 할 때 대상 메소드의 접근 제한자를 지정할 수 있는 옵션이다.

설정할 수 있는 값은 다음 4가지를 설정할 수 있다.

    public
    protected
    private
    none


예를 들어 설정값에 public을 설정하면 다음과 같은 클래스에서 public 접근자를 가진 *sample1* 메소드만 프로파일 대상이 된다.

    public class SampleClass {
        public void sample1() {}
        private void sample2() {}
    }

---

  * 기본값 : 없음
  * 형식 : 문자열(줄바꿈을 이용하여 복수 설정이 가능함)
  * 재시작 : 필요
  * 지원 버전 : 5.0.0