일반적으로 애플리케이션 이름을 설정하는데 설정한 메소드가 반복해서 호출되는 경우 제일 마지막에 호출된 값으로 이름이 설정된다.

해당 옵션을 설정하면 메소드가 반복해서 호출되어도 처음 호출된 메소드의 값을 애플리케이션 이름으로 설정하는 옵션이다.

---

* 기본값 : 없음
* 형식 : 문자열
* 재시작 : 필요
* 지원 버전 : 5.6.0.7, 5.6.1