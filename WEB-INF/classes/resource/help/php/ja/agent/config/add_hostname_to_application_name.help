この値をtrueに設定すると、$_SERVER['SERVER_HOST']がアプリケーション名に追加されます。例えば、www.example.com/index.phpが呼び出されると、設定値がfalseの場合、アプリケーション名はindex.phpとなります。trueに設定すると、example.com/index.phpとなります。

---

* デフォルト値： false
* タイプ：Boolean
* 再起動：不要
* サポートバージョン：5.0.6