설정된 IP로 들어오는 모든 요청을 제니퍼 에이전트 측에서 처리하지 않는다. 따라서 웹 애플리케이션은 정상적으로 처리하지만 X-View를 비롯해 제니퍼의 모든 통계 자료에서 누락된다.

---

* 기본값 : 없음
* 형식 : 문자열(줄바꿈을 이용하여 복수 설정이 가능함)
* 샘플값

예) 192.168.0.100으로부터 들어오는 모든 요청을 무시하고 싶은 경우
ip_filter = 192.168.0.100

예) 192.168.100.xxx로부터 들어오는 모든 요청을 무시하고 싶은 경우
ip_filter = 192.168.100.0/24

* 재시작 : 안함
* 지원 버전 : 5.3.1.4