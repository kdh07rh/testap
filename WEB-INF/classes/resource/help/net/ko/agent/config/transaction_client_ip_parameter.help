트랜잭션의 클라이언트 IP를 특정 메소드의 파라미터 값으로 설정하는 옵션이다.

설정은 전체 메소드 디스크립터를 입력한 후 뒷 부분에 "{}" 를 이용하여 몇 번째 파라미터 값을 사용할지 표시해야 한다.
(지원하는 파라미터값은 문자열과 바이트 배열을 지원한다.)

다음은 WebApplication.DefaultPage 타입에 정의된 SetClientIP 메서드의 첫 번째 매개변수를 IP로 설정하는 예제이다.

    WebApplication.DefaultPage SetClientIP {1}


---

* 기본값 : 없음
* 형식 : 문자열
* 샘플값

        example.clientIp getClientIp {1}
        example.clientIp getClientIp {2}

* 재시작 : 필요
* 연관 옵션
    * [transaction_client_ip_return](./agentHelp?key=transaction_client_ip_return&type=1)
* 지원 버전 : 5.6.0.23
