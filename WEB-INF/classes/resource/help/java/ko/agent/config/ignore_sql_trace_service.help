트랜잭션 모니터링 시 SQL 추적이 필요하지 않은 트랜잭션의 서비스 명을 등록하여 SQL 추적을 하지 않도록 하는 옵션이다.

SQL 추적 방식 중 랩퍼(Wrapper)를 사용할때 CLOB이나 BLOB을 사용하는 경우 문제를 일으킨 사례가 있어 이를 피하기 위해 만들어졌으나 JDBC드라이버에서 SQL을 직접 추적하는 경우에는 사용할 필요가 없다.

---

* 기본값 : 없음
* 형식 : 문자열(줄바꿈을 이용하여 복수 설정이 가능함)
* 샘플값

        /example.do

* 재시작 : 안함
* 지원 버전 : 5.0.0