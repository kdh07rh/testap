이 옵션은 트랜잭션이 수행된 시간을 4개의 구간으로 나누어 트랜잭션이 처리된 구간 정보를 보기 위해 사용하는 옵션이다.

**기본값의 단위는 ms(Millisecond)다.

* 기본값 : 0, 1000, 3000, 8000
* 형식 : 숫자형 배열 값(',' 를 이용하여 4개의 값을 입력해야 한다.)
* 샘플값 : 1000, 3000, 5000, 10000 
* 재시작 : 안함
* 지원 버전 : 5.0.7