このオプションを有効にすると、SQLを収集した時にすべての数字と引用符(Single Quotes)に積まれた値を変数で処理してSQLの重複を最小化することができます。

数字は"#"に変換し、引用符に積もった値は"$"で変数処理されます。

次は変換される例です。

    SELECT * FROM EXAMPLE_123 WHERE COL = 'A'
    
    SELECT * FROM EXAMPLE_# WHERE COL = '$'

---

* デフォルト値 : true
* タイプ : Boolean
* 再起動 : 不要
* サポートバージョン: 5.0.7
