テンポラリテーブルを使用するアプリケーションで異なるSQL数がとても多くなるのを防ぐために使用するオプションです。 テンポラリテーブルはテーブル名に数字が含まれていることですが、名称と数字を引き離して収集することで、重複を最小化してSQL追跡ができるようにします。 次は多く使用されるテンポラリテーブルの例です。
SELECT * FROM EXAMPLE_20150101
    SELECT * FROM EXAMPLE_20150102
    SELECT * FROM EXAMPLE_001
    SELECT * FROM EXAMPLE_002
**テンポラリテーブルは頻繁に使用しないため、確認後にtrueに変更することをお勧めします。**

---

* デフォルト値 : true
* タイプ : Boolean
* 再起動 : 不要
* 関連オプション
    * [enable_parse_sql](./agentHelp?key=enable_parse_sql&type=2)
* サポートバージョン: 5.0.7