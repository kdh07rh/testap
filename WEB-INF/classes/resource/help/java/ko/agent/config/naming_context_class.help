[enable_datasource_trace](./agentHelp?key=enable_datasource_trace&type=1) 옵션이 true로 설정되어 있으면 기본적으로 *javax.naming.InitialContext* 클래스에 대한 추적을 시작한다. 
    
이 클래스를 사용하지 않고 별도로 javax.naming.Context 인터페이스를 구현해서 사용하는 경우에 javax.naming.Context 를 구현한 클래스를 등록해서 사용하려고 할 경우 사용하는 옵션이다.

여기에 설정하는 클래스는 javax.naming.Context 인터페이스를 구현한 클래스만을 지정한다.

**javax.naming.Context의 lookup 메소드를 추적하기 때문에 해당 인터페이스를 구현하지 않는 클래스는 추적이 되지 않는다.**

설정은 고급 옵션을 통해 다음과 같이 설정 할 수 있다.

    com.soft.SampleContext
    com.soft.RealContext
    ...

---

* 기본값 : 없음
* 형식 : 문자열(줄바꿈을 이용하여 복수 설정이 가능함)
* 샘플값

        com.soft.SampleContext
        com.soft.RealContext

* 재시작 : 필요
* 연관 옵션
    * [enable_sql_trace](./agentHelp?key=enable_sql_trace&type=1)
    * [enable_datasource_trace](./agentHelp?key=enable_datasource_trace&type=1)
* 지원 버전 : 5.0.0