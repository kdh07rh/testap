[topology_mode](./agentHelp?type=1&key=topology_mode)가 활성화 되었을 경우 HTTP Header를 통해서 트랜잭션 연결 정보를 전달하는데 사용하는 HTTP Header 키를 설정한다.

기본 키 값은 문자열 "X-J_HTTP_TUID_" 으로 지정 되어 있지만 동일한 키가 이미 사용되고 있을 경우 다른 이름으로 키 값을 변경 할 수 있다.  

---

* 기본값 : X-J_HTTP_TUID_
* 형식 : 문자열
* 샘플값 : X-CUSTOM_TOPOLOGY_ID_
* 재시작 : 안함
* 연관 옵션
    * [topology_mode](./agentHelp?key=topology_mode&type=1)
* 지원 버전 : 5.6.1.18