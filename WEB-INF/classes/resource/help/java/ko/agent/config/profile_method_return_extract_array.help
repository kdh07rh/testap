프로파일 대상 메서드의 리턴이 배열일 경우 그 배열의 값들을 보여준다. 

배열이 아주 큰 경우 수집되는 텍스트 데이터의 크기가 커질수 있기 때문에 원하는 데이터를 확인 후 다시 false로 변경하는 것을 권장한다.

옵션을 true로 설정하면 프로파일의 RETURN 정보가 다음과 같은 형태로 기록된다.

    [0004][14:37:26 303][    0][     ] RETURN [
    type=Object[], size=3
    1:type=String, value=hello
    2:type=String, value=world
    3:type=String, value=aries
    ] java.lang.Object[] org.apache.jsp.methodtest_jsp.pleaseMakeIt(String,String,Object[])

---

* 기본값 : false
* 형식 : Boolean
* 재시작 : 안함
* 지원 버전 : 5.0.14