이 옵션을 활성화하면 SQL을 수집할 때 모든 숫자와 작은 따옴표(Single Quotes)로 쌓여진 값을 변수처리 하여 SQL의 중복을 최소화 하여 저장할지 여부를 설정하는 옵션이다.

숫자는 "#" 으로 변환하고 작은 따옴표에 쌓여진 값은 "$" 로 변수처리 된다.

다음은 변환되는 예제이다.

    SELECT * FROM EXAMPLE_123 WHERE COL = 'A'
    
    SELECT * FROM EXAMPLE_# WHERE COL = '$'

---

* 기본값 : true
* 형식 : Boolean
* 재시작 : 안함
* 지원 버전 : 5.0.0