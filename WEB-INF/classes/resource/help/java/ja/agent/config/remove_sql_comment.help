このオプションがtrueの場合、SQLクエリに含まれているコメントを消去します。
例えば、次のSQLクエリは
    /* 12345abcde */ SELECT * FROM TABLE

オプションを提供すると次のように収集されます。

    SELECT * FROM TABLE

コメントにランダムで生成されるコードまたは文字列がある場合、その文字列が全部保存されて性能に影響を与える際に使用するオプションです。

---

* デフォルト値 : false
* タイプ : Boolean
* 再起動 : 不要
* サポートバージョン : 5.4.3.2