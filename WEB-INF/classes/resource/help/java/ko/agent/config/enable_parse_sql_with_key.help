이 옵션은 특정 프레임워크를 사용해서 SQL 실행시 키를 이용한 변수 바인딩 방식을 지원 하는데
실제 실행은 java.sql.PreparedStatement 을 이용하여 키 값을 "?" 로 변환하여 SQL 을 실행한다.

다음은 사용되는 SQL 예제이다.

    INSERT INTO table VALUES (:ID, :NAME...) 
    SELECT * FROM table WHERE ID = :ID AND NAME = :NAME 

이런 SQL 문은 기본 JDBC 스펙에는 없는 방법으로 실제 SQL 실행시에 프레임워크에서 ":KEY => ?" 로 변환하는 방법을 사용하기 때문에
변환된 내용은 프로파일을 통해서 확인 할 수 있다.

옵션을 활성화하면 이 키를 파싱하여 화면상에서 SQL 빌더를 통하여 실행 할 수 있도록 해준다.

    자바에서는 기본으로 사용하지 않는 방법이라 기본값이 false 처리 되어 있다.
    이를 true로 설정하면 SQL 파싱을 한번 더 수행하기 때문에 성능에 민감한 경우라면 반드시 확인후 사용해야 한다.

---

* 기본값 : false
* 형식 : Boolean 
* 재시작 : 안함 
* 연관 옵션
    * [enable_sql_trace](./agentHelp?key=enable_sql_trace&type=1)
* 지원 버전 : 5.0.9.1