HTTP Header에 포함된 값을 이용하여 애플리케이션명을 설정할 경우 사용하는 옵션이다.
관리화면을 통해 일반적인 설정이 가능하나 특정 상황에 HTTP Header의 특정 값을 애플리케이션명으로 설정하고자 할 경우 사용한다.

사용시 주의 할 점은 애플리케이션명 설정을 관리 화면과 중복 설정 할 경우 관리 화면에서 설정하는 값이 우선순위가 높아 HTTP Header를 이용해 설정된 애플리케이션명을 덮어 쓰게 된다.

---

* 기본값 : 없음
* 형식 : 문자열
* 샘플값 : service_name
* 재시작 : 안함
* 지원 버전 : 5.6.1.21