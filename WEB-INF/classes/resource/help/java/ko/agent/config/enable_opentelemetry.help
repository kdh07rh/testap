에이전트 설정 파일에 설정하는 OpenTelemetry와 통합을 위한 옵션이다.

옵션을 활성화해서 사용하려면 두 가지를 해야 하는데 먼저 데이터 서버에 OpenTelemetry 데이터를 수집하기 위한 옵션을 설정해야 한다.

jennifer_data.sh 설정 파일에 다음과 같은 옵션이 설정되어 있어야 데이터를 수집할 수 있다.

    JAVA_OPTS=" ${JAVA_OPTS} -Djennifer.unofficial.opentelemetry.collect.port=4317"
    
여기서 설정된 포트는 [server_grpc_port](./agentHelp?key=server_grpc_port&type=1)에 설정해야 하는 grpc 포트이다.

다음으로는 OpenTelemetry 에이전트를 추가로 설정해 줘야 한다.

    JENNIFER_JAVA_OPTS=" -javaagent:${JENNIFER_HOME}/agent.java/jennifer.jdk8.jar" 
    JENNIFER_JAVA_OPTS="${JENNIFER_JAVA_OPTS} -javaagent:${JENNIFER_HOME}/agent.java/ext/opentelemetry-javaagent.jar"

    
이 과정이 포함된 상태에서 옵션을 활성화했을 때 정상적인 동작을 한다. 
    
---

* 기본값 : false
* 형식 : Boolean
* 재시작 : 필요
* 연관 옵션
    * [server_address](./agentHelp?key=server_address&type=1)
    * [server_grpc_port](./agentHelp?key=server_grpc_port&type=1)
* 지원 버전 : 5.6.1.16, 5.6.2