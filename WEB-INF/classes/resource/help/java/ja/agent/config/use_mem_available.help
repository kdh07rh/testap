システムメモリ使用率を調べる時、Linuxの場合、/proc/meminfoファイルを利用します。
この時、使用可能なメモリ(free)については"MemFree"カラムを使いますが、この値には"Buffers/Cached"値が含まれていません。

このため、使用率が高く出る場合がありますが、この数値を計算する時、"MemFree"値の代わりに"MemAvailable"値を使うようにするオプションです。

---

* デフォルト値 : false
* 形式 : Boolean
* 再起動 : しない
* サポートバージョン：5.6.2.7