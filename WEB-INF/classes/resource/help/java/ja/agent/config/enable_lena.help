LG CNS LENA WASをサポートするためのオプションです。

LENA WASは、2つのプロセスが実行され、モニタリングが必要ない管理プロセスに対してエージェント接続をしないために使用するオプションです。

システムプロパティの"agent.conf"プロパティで管理プロセスを区別します。

--- ---。

* 基本値 : false
* 形式 : Boolean
* 再起動：必要
* サポートバージョン：5.6.2.13