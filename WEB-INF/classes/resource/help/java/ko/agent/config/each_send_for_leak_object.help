기본값은 JDBC Statement, ResultSet 객체에 대한 Leak 체크시 발생한 Leak 에 대한 정보를 일괄로 보내면서 감지된 횟수를 메시지에 보내는 형태이다.

다음은 발생하는 메시지의 예제이다.

    JDBC NOT CLOSE : com.mysql.jdbc.JDBC4PreparedStatement count = 1
    JDBC NOT CLOSE : com.mysql.jdbc.JDBC4ResultSet count = 5


위 처럼 일괄로 보낼 경우 실제 에러 건수가 일치 하지 않을 수 있는데 발생하는 에러 건수가 중요한 경우 이를 맞추기 위해 개별로 보내도록 설정하는 옵션이다.

주의 할 점은 개별로 에러 건수를 보내면 에러 건수가 너무 많아질 수 있기 때문에 이 부분을 고려해서 사용해야 한다.

---

* 기본값: false
* 형식 : Boolean
* 재시작 : 안함
* 연관 옵션
    * [enable_statement_leak_trace](./agentHelp?key=enable_statement_leak_trace&type=1)
    * [enable_resultset_leak_trace](./agentHelp?key=enable_resultset_leak_trace&type=1)
* 지원 버전 : 5.3.1