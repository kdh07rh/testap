이 옵션은 JDK 8 이상 사용시 Java 람다(Lambda) 표현식으로 작성한 메소드를 프로파일링 할 때 사용하는 옵션이다.

람다 표현식으로 작성한 클래스에는 일반적으로 다음과 같이 클래스가 작성된다.

    Example$$Lambda$1


기본적으로 익명 클래스에 대해서 프로파일링 하지 못하도록 되어 있기 때문에 설정파일에 다음 옵션과 함께 사용해야 정상적으로 사용할 수 있다.

    _ignore_anonymous_class = false


주의 할 점은 옵션을 사용한다고 해도 모든 람다 표현식과 익명 클래스에 대해서 프로파일링이 가능한건 아니기 때문에 꼭 확인 후에 사용해야 한다. 

---

* 기본값 : false
* 형식 : Boolean
* 재시작 : 필요
* 지원 버전 : 5.4.0