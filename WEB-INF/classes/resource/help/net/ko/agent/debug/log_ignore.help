로그 파일에 남는 내용은 INFO, DEBUG, ERROR로 나뉘는데, 그 중에서 제외하고 싶은 분류를 지정한다. 가령, INFO, DEBUG, ERROR 값은 정수로 다음과 같고,

    INFO = 1
    DEBUG = 2
    ERROR = 3

만약 log_ignore 값을 3으로 지정한다면,

    log_ignore = 3

이후 Error 등급에 해당하는 로그는 모두 제외된다.

---
    
* 기본값 : 없음
* 형식 : 숫자형
* 샘플값 : 3
* 재시작 : 없음
* 지원 버전 : 5.0.7