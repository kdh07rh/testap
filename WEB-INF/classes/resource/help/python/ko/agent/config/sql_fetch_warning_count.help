이 옵션은 SQL 수행후 데이터 조회(FETCH)가 많이 발생할 경우 ERROR를 발생시키기 위한 옵션이다.

기본값은 "10000"으로 설정된 값을 초과한 데이터 조회(FETCH)가 발생하면 SQL_TOOMANY_FETCH ERROR 이벤트가 발생한다.

---

* 기본값 : 10000
* 형식 : 숫자(Number)
* 샘플값 : 1000 
* 재시작 : 안함
* 지원 버전 : 5.6.1.21