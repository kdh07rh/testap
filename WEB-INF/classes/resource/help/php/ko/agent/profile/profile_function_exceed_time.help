수행시간이 profile_function_exceed_time를 초과하는 메소드, 함수를 자동으로 프로파일한다. 1/1000초 단위로 profile_function_exceed_time을 설정해야 한다. 이 값이 -1이면 자동 프로파일 기능이 활성화되지 않는다. 단 에러가 발생한 함수와 해당 함수 콜 트리 노드는 profile_function_exceed_time으로 수집되지 않는다.profile_target 등을 사용해 명시적으로 프로파일을 한다면 해당 함수가 콜트리 노드로 표시된다

만약 이 값을 10으로 설정하면 10ms 이상인 메소드, 함수를 자동으로 프로파일한다.

---

* 기본값 : -1
* 형식 : 숫자(Number)
* 샘플값 : 300
* 재시작 : 안함
* 지원버전 : 5.0.0