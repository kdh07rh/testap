ログファイルの保存期間を設定します。

0 ~ 365の値を設定できます。単位は日数です。

デフォルト値(0)の場合はログファイルを削除せず永久保存します。
次のように設定した場合、今日基準7日以前ログファイルが削除されます。

    ex) log_max_age = 7


---

  * デフォルト値 : 0
  * タイプ : 数字(Number)
  * 再起動 : 不要
  * サポートバージョン : 5.6.1.22