SQL収集時にすべての数字およびシングルクォート内の値を変数として処理を行い、SQLの重複を最小化するために使用するオプションです。

数値は'#に、シングルクォートは'$'の変数に変換します。

以下は、変換の例です。

    SELECT * FROM EXAMPLE_123 WHERE COL = 'A'
    
    SELECT * FROM EXAMPLE_# WHERE COL = '$'

---

* デフォルト値： true
* タイプ：Boolean
* 再起動：不要
* サポートバージョン：5.0.0