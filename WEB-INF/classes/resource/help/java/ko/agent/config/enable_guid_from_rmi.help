RMI를 이용하여 원격 메서드 호출 요청을 받은 경우(즉, 서버의 입장) 호출한 측으로부터 넘어온 guid를 현재 트랜잭션에 설정할 것인지를 결정하는 옵션이다.

guid를 넘겨받지 않아도 RMI 호출에 대하여 XView 팝업의 연관 트랜잭션, 대시보드의 토폴로지에 표현이 가능하나 사용자가 원할 경우 guid를 넘겨 받을 수 있도록 했다.

기본값은 false로 이 값을 true로 지정하면 전달받은 GUID를 현재 트랜잭션의 GUID로 이용한다.

물론 호출한 측의 트랜잭션에 GUID 값이 있어야 하며 정상적으로 값을 전달 받으면 프로파일에 다음과 같은 내용이 남는다.

    [0001][14:33:50 398][    0][     ] SET GUID(from RMI)=2506379062092865680

---

* 기본값 : false
* 형식 : Boolean
* 재시작 : 안함
* 연관 옵션
    * [trace_rmi](./agentHelp?key=trace_rmi&type=1)
    * [guid_max_length](./agentHelp?key=guid_max_length&type=1)
* 지원 버전 : 5.0.12