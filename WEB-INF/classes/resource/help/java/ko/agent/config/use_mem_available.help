시스템 메모리 사용률을 구할 때 리눅스인 경우 /proc/meminfo 파일을 이용한다.
이때 사용가능한 메모리(free)에 대해서는 "MemFree" 컬럼을 사용하는데 이 값에는 "Buffers/Cached" 값이 포함되어 있지 않다.

이로 인해서 사용률이 높게 나오는 경우가 있는데 이 수치를 계산할 때 "MemFree" 값 대신 "MemAvailable" 값을 이용하도록 하는 옵션이다.

---

* 기본값 : false
* 형식 : Boolean
* 재시작 : 안함
* 지원 버전 : 5.6.2.7