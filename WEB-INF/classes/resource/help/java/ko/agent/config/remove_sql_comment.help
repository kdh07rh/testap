이 옵션 값이 true면 SQL 쿼리 문에 포함된 주석을 제거한다.

다음과 같은 쿼리가 있을 경우 주석을 제거 해서 저장하게 된다.

    /* 12345abcde */ SELECT * FROM TABLE

옵션 적용후 아래와 같이 보인다.

    SELECT * FROM TABLE


주석에 랜덤으로 생성되는 코드나 문자열이 포함될 경우 문자열이 캐시되지 않고 전부 저장되어 성능에 영향을 미칠 때 사용하는 옵션이다.

---

* 기본값 : false
* 형식 : Boolean
* 재시작 : 안함
* 지원 버전 : 5.4.3.2